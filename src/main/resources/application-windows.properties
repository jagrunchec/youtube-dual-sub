# ── Windows profile overrides ────────────────────────────────────
# Activate with: java -jar ... --spring.profiles.active=windows
# (optional — application.properties already defaults to Windows paths)

app.transcript.python=C:/Python314/python.exe

# Mail: configure your Gmail app password here.
app.mail.enabled=false
# spring.mail.username=VOTRE_EMAIL@gmail.com
# spring.mail.password=VOTRE_MOT_DE_PASSE_APPLICATION
